# Cámara virtual para OBS: solo CAPTURE mientras iso_capture escribe.
# exclusive_caps=1 evita el crash de linux-v4l2.so en OBS 30
# (free(): invalid pointer al listar un loopback OUTPUT+CAPTURE).
options v4l2loopback video_nr=42 card_label=ElgatoHD60S exclusive_caps=1
